package juegomovimiento;

/**
 * Esta es la interfaz Estrategia (Strategy).
 * Define el método que todas las estrategias concretas deben implementar.
 */
public interface EstrategiaMovimiento {
    
    // Método abstracto (sin cuerpo) que define la operación
    void moverse();
}
